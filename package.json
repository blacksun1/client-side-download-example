{
  "name": "client-side-download-example",
  "version": "0.0.0",
  "description": "Example of export from a table using only the client",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify src/main.js -o files/bundle.js",
    "start": "node .",
    "prestart": "npm run build"
  },
  "author": "blacksun1 <blacksun1@users.noreply.github.com>",
  "license": "MIT",
  "dependencies": {
    "csv-stringify": "^1.0.2",
    "uniq": "^1.0.1"
  }
}
